%ul.providers.list.inline
  - Secrets::defined_providers[0,7].each_with_index do |provider,i|
    %li
      %a.auth_provider{ href: "/auth/#{provider.to_s.downcase}", class:'btn-social ', data: {"no-turbolink" => true} }
        = social_icon(provider)
  - if Secrets::defined_providers.drop(7).length > 0
    %li.dropdown
      %ul.dropdown-menu
        - Secrets::defined_providers.drop(7).each do |p|
          %li
            %a.auth_provider{ href: "/auth/#{p.to_s.downcase}", data: {"no-turbolink" => true} }
              = p.to_s.downcase
      = link_to '#', class:'dropdown-toggle btn-social', data:{toggle:'dropdown', target:'#'} do
        %i.icon-plus-sign

